//! `kimmy` — terminal client for KimmyDB. Landing in M5.

fn main() -> anyhow::Result<()> {
    println!("kimmy: the interactive client lands in M5; use curl against the HTTP API for now");
    Ok(())
}
